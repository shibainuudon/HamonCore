name: condition_variable

on:
  push:
    paths:
      - libs/condition_variable/**
      - .github/workflows/condition_variable.yml
      - .github/workflows/build.yml

  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      src_dir: libs/condition_variable
